# EduTesting

EduTesting — веб-приложение для онлайн-тестирования студентов на `Django`.
Проект сочетает простой интерфейс на `Django Templates` и сильную серверную часть: автоматическую проверку попыток, аналитику по темам, персональные условия, апелляции, уведомления, достижения и контроль подозрительных попыток.

## Автор и права

- автор проекта: `Кашпирев М.Д.`
- все права на исходный код, структуру проекта, документацию и сопроводительные материалы принадлежат автору;
- распространение и использование материалов вне личного проекта автора допускается только с его разрешения.

## Почему проект выглядит зрелым

- закрывает полный цикл онлайн-тестирования: от публикации курса до аналитики и апелляций;
- сочетает простой серверный интерфейс и насыщенный backend без перегрузки фронтенда;
- поддерживает и web-сценарии, и REST API со Swagger UI и Postman;
- опирается на MVT-архитектуру, Django ORM, сервисный и аналитический слои;
- покрыт автотестами и демонстрационными данными для быстрого запуска.

## Что умеет система

### Для студента

- подключение к курсу;
- прохождение тестов с ограничением по времени и попыткам;
- автосохранение черновика попытки;
- просмотр подробного результата с разбором ответов;
- сравнение с предыдущей попыткой;
- подача апелляции;
- просмотр уведомлений;
- личные достижения по попыткам и курсам.

### Для преподавателя

- создание и публикация курсов, тестов, вопросов и вариантов ответов;
- публикация объявлений;
- просмотр всех завершенных попыток по тесту;
- комментарии к попыткам;
- выдача индивидуальных условий по тесту;
- аналитика по темам и рейтинг по курсу;
- список студентов, которым нужно внимание;
- контроль подозрительных попыток по курсу;
- экспорт результатов курса в `CSV`.

### Сильные backend-фичи

- диагностика по темам после завершения попытки;
- сравнение результата с предыдущей попыткой;
- вычисляемые достижения без усложнения фронтенда;
- флаги контроля честности:
  - идеальный результат за короткое время;
  - слишком быстрое завершение;
  - резкий скачок между попытками;
- преподавательская сводка по подозрительным попыткам;
- API со Swagger и готовой коллекцией Postman.

## Соответствие требованиям

| Требование | Как реализовано в проекте |
| --- | --- |
| Веб-сервис онлайн-тестирования студентов | Курсы, тесты, попытки, результаты, апелляции, уведомления и аналитика |
| `Python` и `Django` | Основной стек проекта |
| `Django Templates` | Весь клиентский слой построен на серверных шаблонах |
| `SQLite3` | База данных по умолчанию в `student_testing/settings.py` |
| Паттерн `MVT` | Разделение на `models`, `views`, `templates`, плюс сервисный и аналитический слои |
| Межстраничная навигация | Главная, курсы, панель, курс, тест, результат, уведомления, Swagger |
| Современный web-интерфейс | Единый стиль, простая навигация, адаптация новых функций без перегруза страниц |
| Серверная логика | `testing/views.py`, `testing/services.py`, `testing/analytics.py`, `testing/api_views.py` |
| Логика базы данных | `accounts/models.py`, `testing/models.py`, миграции и `Django ORM` |
| Клиентское представление | `templates/` и `static/css/` |

## Технологии

- `Python`
- `Django`
- `Django ORM`
- `SQLite3`
- `Django REST Framework`
- `drf-spectacular`
- `Swagger UI`
- `Django Templates`
- `HTML + CSS`

## Что хранится в базе данных

В `db.sqlite3` хранится вся предметная модель сервиса: пользователи, курсы, тесты, вопросы, ответы, попытки, уведомления и служебные данные для расширенных backend-сценариев.

Основные сущности:

- `User` — учетная запись, роль, почта и учебная группа;
- `Course` — учебный курс и его описание;
- `Enrollment` — запись студента на курс;
- `Announcement` — объявления внутри курса;
- `Quiz` — тест курса и его настройки;
- `Question` — вопрос теста;
- `Choice` — вариант ответа на вопрос;
- `Attempt` — попытка прохождения теста;
- `AttemptAnswer` — выбранные ответы внутри попытки;
- `AttemptReview` — комментарий преподавателя к попытке;
- `AttemptDraft` — автосохраненный черновик попытки;
- `AttemptAppeal` — апелляция студента по результату;
- `UserNotification` — уведомления пользователя;
- `QuizAccessOverride` — индивидуальные условия по времени и попыткам.

## Архитектура

Проект построен по паттерну `MVT`.

- `models` — предметные сущности и расчётные свойства;
- `views` — web-flow преподавателя и студента;
- `templates` — простой интерфейс без SPA;
- `services.py` — сервисные сценарии вроде проверки попытки, автосохранения и уведомлений;
- `analytics.py` — рейтинг, диагностика по темам, сравнение попыток, достижения и контроль честности;
- `api_views.py` / `api_serializers.py` — REST API и OpenAPI-описание.

## Wiki проекта

Для релиза и демонстрации подготовлена отдельная wiki-навигация:

- [docs/wiki/index.md](docs/wiki/index.md) — точка входа в wiki;
- [docs/wiki/domain.md](docs/wiki/domain.md) — предметная область и роли;
- [docs/wiki/architecture.md](docs/wiki/architecture.md) — архитектура и MVT;
- [docs/wiki/backend.md](docs/wiki/backend.md) — серверная логика и backend-фичи;
- [docs/wiki/database.md](docs/wiki/database.md) — слой базы данных и сущности;
- [docs/wiki/frontend.md](docs/wiki/frontend.md) — клиентский слой и навигация;
- [docs/wiki/api.md](docs/wiki/api.md) — API, Swagger UI и Postman;
- [docs/wiki/release-checklist.md](docs/wiki/release-checklist.md) — релизный чеклист и соответствие требованиям.

## Быстрый запуск

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python manage.py migrate
python manage.py seed_demo_data
python manage.py runserver
```

После запуска:

- web-интерфейс: `http://127.0.0.1:8000/`
- Swagger UI: `http://127.0.0.1:8000/api/docs/`
- OpenAPI schema: `http://127.0.0.1:8000/api/schema/`

## Тестовые аккаунты

### Основные

- преподаватель: `teacher_demo` / `TeacherDemo123!`
- студент: `student_demo` / `StudentDemo123!`

### Дополнительные

- преподаватель: `teacher_test` / `TeacherTest123!`
- студент: `student_test` / `StudentTest123!`

## Демо-данные

Команда:

```bash
python manage.py seed_demo_data
```

Создает насыщенный набор данных для демонстрации:

- преподавателей и студентов;
- курсы, тесты, вопросы и варианты ответов;
- завершенные попытки;
- комментарии преподавателя;
- уведомления;
- индивидуальные условия по тестам;
- примеры апелляций;
- материал для аналитики, достижений и контроля подозрительных попыток.

## Ключевые web-маршруты

- `/` — главная страница
- `/courses/` — каталог курсов
- `/dashboard/` — рабочая панель
- `/courses/<id>/` — страница курса
- `/courses/<id>/insights/` — диагностика курса
- `/quizzes/<id>/` — страница теста
- `/quizzes/<id>/attempts/` — попытки студентов по тесту
- `/attempts/<id>/result/` — результат попытки
- `/notifications/` — центр уведомлений

## Ключевые API-маршруты

### Базовые

- `POST /api/auth/token/`
- `GET /api/me/`
- `GET /api/stats/`
- `GET /api/courses/`
- `GET /api/courses/<id>/`
- `GET /api/quizzes/<id>/`

### Студент

- `GET /api/my/courses/`
- `GET /api/my/achievements/`
- `POST /api/courses/<id>/enroll/`
- `POST /api/quizzes/<id>/start/`
- `POST /api/attempts/<id>/draft/`
- `POST /api/attempts/<id>/submit/`
- `GET /api/attempts/<id>/`
- `POST /api/attempts/<id>/appeal/`

### Преподаватель

- `GET /api/courses/<id>/analytics/`
- `GET /api/courses/<id>/integrity/`
- `GET /api/quizzes/<id>/attempts/`
- `GET /api/quizzes/<id>/overrides/`
- `POST /api/quizzes/<id>/overrides/`
- `POST /api/appeals/<id>/review/`

## Swagger и Postman

В проекте уже подготовлены материалы для проверки API:

- [schema.yaml](schema.yaml) — OpenAPI schema;
- [docs/api_demo.md](docs/api_demo.md) — краткий сценарий демонстрации API;
- [docs/EduTesting.postman_collection.json](docs/EduTesting.postman_collection.json) — готовая коллекция Postman;
- [docs/EduTesting.postman_environment.json](docs/EduTesting.postman_environment.json) — локальное environment для Postman.


- Swagger UI содержит подробные описания сценариев, теги, примеры тел запросов и готовые точки для `Try it out`;
- Postman-коллекция покрывает публичные, студенческие и преподавательские API-сценарии;
- в Postman добавлены автоматические проверки ответов и сохранение ключевых идентификаторов вроде `attempt_id` и `appeal_id`;
- эти материалы подходят для скриншотов, показывающих фактическую работоспособность API.

## Тестирование

Запуск всех автотестов:

```bash
python manage.py test
```

Запуск тестов приложения `testing`:

```bash
python manage.py test testing
```

Проверяются:

- модели и формы;
- сервисы проверки попыток и автосохранения;
- аналитика по темам;
- достижения и контроль подозрительных попыток;
- web-flow студента и преподавателя;
- права доступа;
- API, Swagger и OpenAPI schema.

## Кодировка и качество текста

Чтобы русский текст стабильно отображался в коде, документации и шаблонах:

- добавлен [.editorconfig](.editorconfig) с `charset = utf-8`;
- добавлен [.gitattributes](.gitattributes) с едиными правилами для текстовых файлов;
- в основных HTML-шаблонах используется `<meta charset="utf-8">`;
- OpenAPI schema и Swagger UI синхронизированы с текущими русскоязычными описаниями.

## Перспективы развития

- подключение `PostgreSQL` и подготовка production-конфигурации с разделением окружений;
- фоновая обработка уведомлений и публикаций через `Celery` или аналогичный брокер задач;
- импорт вопросов и тестов из `CSV` / `XLSX` с валидацией структуры;
- расширение контроля честности попыток: журнал действий, аудит и более точные эвристики;
- персональные учебные рекомендации на основе слабых тем и истории попыток;
- интеграция с внешними учебными платформами и системами авторизации;


## Структура проекта

```text
KyrsBACK/
|-- accounts/
|-- docs/
|-- static/
|-- student_testing/
|-- templates/
|-- testing/
|-- manage.py
|-- requirements.txt
`-- schema.yaml
```

## Документы проекта

- [docs/wiki/index.md](docs/wiki/index.md)
- [docs/wiki/release-checklist.md](docs/wiki/release-checklist.md)
- [docs/project_report.md](docs/project_report.md)
- [docs/api_demo.md](docs/api_demo.md)
